import { FilterVisitorPipe } from './filter-visitor.pipe';

describe('FilterVisitorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVisitorPipe();
    expect(pipe).toBeTruthy();
  });
});
