import { ExcelUploadExpenseErrorMessage } from './excel-upload-expense-error-message';

describe('ExcelUploadExpenseErrorMessage', () => {
  it('should create an instance', () => {
    expect(new ExcelUploadExpenseErrorMessage()).toBeTruthy();
  });
});
