import { NotificationListArray } from './notification-list-array';

describe('NotificationListArray', () => {
  it('should create an instance', () => {
    expect(new NotificationListArray()).toBeTruthy();
  });
});
