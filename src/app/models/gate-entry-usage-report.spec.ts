import { GateEntryUsageReport } from './gate-entry-usage-report';

describe('GateEntryUsageReport', () => {
  it('should create an instance', () => {
    expect(new GateEntryUsageReport()).toBeTruthy();
  });
});
