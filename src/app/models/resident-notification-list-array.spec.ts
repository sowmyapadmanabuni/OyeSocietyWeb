import { ResidentNotificationListArray } from './resident-notification-list-array';

describe('ResidentNotificationListArray', () => {
  it('should create an instance', () => {
    expect(new ResidentNotificationListArray()).toBeTruthy();
  });
});
