import { InvoiceDescriptionListOne } from './invoice-description-list-one';

describe('InvoiceDescriptionListOne', () => {
  it('should create an instance', () => {
    expect(new InvoiceDescriptionListOne()).toBeTruthy();
  });
});
