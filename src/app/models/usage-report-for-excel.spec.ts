import { UsageReportForExcel } from './usage-report-for-excel';

describe('UsageReportForExcel', () => {
  it('should create an instance', () => {
    expect(new UsageReportForExcel()).toBeTruthy();
  });
});
